# hello-world

Datum: 2026-07-09

| Thema | Erkenntnis |
|-------|------------|
| Methodendefinition | Eine Methode in Java besteht aus Rueckgabetyp (`String`), Name (`getGreeting`) und `return`. |
| Aufbau Exercism | Projekt hat `src/main/java/` fuer die Loesung und `src/test/java/` fuer die Tests. Build-Tool ist Gradle. |
| Strings in Java | Strings werden in doppelten Anfuerungszeichen geschrieben. |

